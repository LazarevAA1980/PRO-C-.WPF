<UserControl x:Class="MainWpfApp.Views.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainWpfApp.Views.Settings"
             xmlns:vm="clr-namespace:MainWpfApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:SettingsViewViewModel/>
    </UserControl.DataContext>
    <Grid>
        <StackPanel HorizontalAlignment="Center" Width="200">
            <TextBlock>
            Temperature measure units
            </TextBlock>
            <TextBox/>
            <TextBlock>
            Pressure measure units
            </TextBlock>
            <TextBox/>
            <TextBlock>
            Precipitation measure units
            </TextBlock>
            <TextBox/>
            <TextBlock>
            Wind speed measure units
            </TextBlock>
            <TextBox/>
            <TextBlock>
            Language
            </TextBlock>
            <TextBox/>
        </StackPanel>
    </Grid>
</UserControl>
